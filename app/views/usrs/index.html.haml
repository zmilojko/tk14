%h1 Listing users

%table
  %tr
    %th Email
    %th
    %th
    %th

  - @usrs.each do |usr|
    %tr
      %td= usr.email
      %td= link_to 'Show', usr_path(usr)
      %td= link_to 'Edit', edit_usr_path(usr)
      %td= link_to 'Destroy', usr_path(usr), :method => :delete, :data => { :confirm => 'Are you sure?' }
      %td{:style => "font-weight: bold;"}= "(Admin)" if usr.is_admin?

%br

= link_to 'New User', new_usr_path
