%h1 Listing categories

%table
  %tr
    %th Code
    %th Description
    %th Length
    %th
    %th
    %th

  - @categories.each do |category|
    %tr
      %td= category.code
      %td= category.description
      %td= category.length
      %td= link_to 'Show', category
      %td= link_to 'Edit', edit_category_path(category) if current_user.try(:is_admin?)
      %td= link_to 'Destroy', category, :method => :delete, :data => { :confirm => 'Are you sure?' } if current_user.try(:is_admin?)

%br

= link_to 'New Category', new_category_path if current_user.try(:is_admin?)
